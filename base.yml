---
- name: Base configuration
  hosts: all
  become: true
  roles:
    - locale
    - swap
    - packages
    - systemd-resolved
    - kernel
    - timezone_chrony
    - exim4
    - user_profiles
    - ssh
    - logrotate
